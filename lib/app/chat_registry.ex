defmodule App.ChatRegistry do
  @moduledoc """
  Registry para salas de chat por pedido.
  """
  # Este módulo serve como alias do Registry.
  # O Registry deve ser iniciado no supervision tree da aplicação.
end
