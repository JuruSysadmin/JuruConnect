<!DOCTYPE html>
<html lang="pt-BR" data-theme="light" class="[scrollbar-gutter:stable]">
  <head>
    {Application.get_env(:live_debugger, :live_debugger_tags)}

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix="  Dashboard">
      <%= assigns[:page_title] || "" %>
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>


  </head>
  <body class="bg-base-100 antialiased min-h-screen">
    <!-- Header com informações do usuário (apenas se logado) -->
    <%= if assigns[:current_user] do %>
      <div class="navbar bg-base-100 shadow-sm border-b border-base-300">
        <div class="max-w-7xl mx-auto w-full px-4 sm:px-6 lg:px-8">
          <div class="navbar-start">
            <span class="text-xl font-bold text-primary">JuruConnect</span>
          </div>
          <div class="navbar-end">
            <div class="flex items-center space-x-4">
              <div class="text-sm text-base-content">
                Olá, <span class="font-medium"><%= get_user_display_name(@current_user) %></span>
                <%= if get_user_role(@current_user) do %>
                  <span class="text-base-content/70">(<%= get_user_role(@current_user) %>)</span>
                <% end %>
              </div>
              <a
                href="/logout"
                class="text-sm text-error hover:text-error/80 font-medium transition-colors duration-200"
              >
                Sair
              </a>
            </div>
          </div>
        </div>
      </div>
    <% end %>
    <main class="w-full h-full min-h-screen">
      <.flash_group flash={@flash} />
      <%= @inner_content %>
    </main>
  </body>
</html>
